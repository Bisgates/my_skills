# 3_state — 当前状态

> 覆写文件，不是流水账。历史在 `0_meta.md ## log`，这里只回答"现在是什么状态"。
> 接手这个 arc 的 agent 先读它，再读 `1_objective.md`。
> `/arc-execute` 每完成一个 plan 步骤覆写一次。

## 现在在哪

<plan 第几步 / 在做什么 / 卡在哪。一两句，写完覆盖上一版。>

## 怎么重跑

```bash
# env:   <conda env / venv / 容器>
# 数据:  <绝对路径>
# smoke: <小数据验证命令>
# full:  <完整运行命令>
```

## 已确定的事实

<!-- 只写"后面不会再推翻"的结论：定下的决策、排除的方案、发现的数据问题。每条一句理由。 -->

- <决策 / 事实> —— <一句为什么>

## 落盘候选

<!-- 写代码的当下就记，别等 finalize 回忆。没有就写 none。 -->

| arc 内路径 | 建议去处 | 为什么 |
|---|---|---|
| `utils/<x>.py` | `<主项目路径>` | <一句> |
