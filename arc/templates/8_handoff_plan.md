# 8_handoff_plan — {{ARC_NAME}}

> Generated by /arc-finalize Stage 1. **Draft only — the agent does not touch the main project.**
> Stage 2 starts only after the user replies `approved` or `approved with changes: ...`.

## 1. Code promote plan
| from (inside arc) | to (main project) | mod type | lines | reason |
|---|---|---|---|---|
| utils/<file>.py | <project>/<path>/<file>.py | new | <N> | <reason> |

> mod type ∈ {new, merge, replace, delete}.

## 2. Doc proposals (follows AGENTS.md protocol)
- [NEW] docs/<sub>/<name>.md — <one-line reason>
- [UPDATE] docs/<...>.md — <one-line summary of the edit>
- [STALE?] docs/<...>.md — <one-line reason it looks stale>

## 3. Skipped items (explicitly not promoted)
- scripts/* (all skipped by default)
- utils/<file>.py — <reason>

## 4. Risks / TODOs
- <known-but-not-done items, items requiring follow-up arcs, or regression risks introduced by this promote>

---

Reply `approved` after review (or `approved with changes: ...`).
