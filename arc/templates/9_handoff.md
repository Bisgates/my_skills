# 9_handoff — {{ARC_NAME}}

> arc `{{ID}}` · {{DATE}}
> 交接文件，不是报告。三块：结论、落盘清单、未尽事项。过程在 `0_meta.md ## log`，不要在这里重写一遍。
> 这个文件是 `done` 的硬门槛：必须存在且非空。

## 结论

<一句话：验收达成没有。>

- **<指标>** <值> <（baseline / target …）>

<1-2 句：留了哪些尾巴 / 哪条路径最后是有效的。>

## 落盘清单

<!-- 从 3_state.md 的「落盘候选」复核而来。这是给用户看的建议，agent 不代为执行。 -->

| arc 内路径 | 建议去处 | 类型 | 为什么 |
|---|---|---|---|
| `utils/<x>.py` | `<主项目路径>` | new / merge / replace | <一句> |

跳过不落盘的：`scripts/`（一次性）、`output/`（实验产物）、`_tmp/`（agent 草稿）。

## 未尽事项

<!-- 下一个 agent 最需要的三类：没做完的、踩过的坑、建议下一个 arc 做什么。没有就写 none。 -->

- <事项>
